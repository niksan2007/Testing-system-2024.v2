<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Страница тестов</title>
    <link rel="stylesheet" href="/css_files/style_watch_test.css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>

<body>
    <h1>Сведения</h1>
    <table width="55%">
        <tr>
            <th>Тема тестирования</th>
            <th>Количество вопросов</th>
            <th>Время тестирования</th>
        </tr>
    
        <%
            if (tests.length > 0) {
                for (let i = 0; i < tests.length; i++) {
                    %>
                    <tr>
                        <td><%= tests[i].topic %></td>
                        <td><%= tests[i].numberQues %></td>
                        <td><%= tests[i].numberRemaining %> минут</td>
                        <td>
                            <a href="/edit-test?id=<%= tests[i]._id %>">Изменить</a>
                            <br><br>
                            <a href="/delete-test?id=<%= tests[i]._id %>">Удалить</a>
                            <br><br>
                        </td>
                    </tr>
                    <%
                }
            } else {
                %>
                <tr>
                    <td colspan="2">Тестов не создано</td>
                </tr>
                <%
            }
        %>
    </table>
    
    <div class="register">
        <a href="/home_lecturer">Вернуться обратно</a>
    </div>

</body>
</html>